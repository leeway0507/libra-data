syntax = "proto3";

package embedding;

option go_package= "/embedding/pb";


message EmbeddingVector {
  string isbn = 1;
  float embedding = 2;  
}

// generate code  : protoc --go_out=. embedding.proto

